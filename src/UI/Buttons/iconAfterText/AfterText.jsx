import { Icon } from '@ant-design/icons';
import React from 'react';

const AfterText = ({ icon }) => {
  return <Icon component={icon} style={{ marginRight: '25px' }} />;
};

export default AfterText;
